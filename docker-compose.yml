version: '3.8'

services:
  yonocytech:
    build: .
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: production
      PORT: 3000
    volumes:
      - ./:/app
      - /app/node_modules
